name: Lean Action CI

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  tall:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - uses: leanprover/lean-action@v1
        with:
          build-args: --wfail
          lake-package-directory: tall

  numan:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - uses: leanprover/lean-action@v1
        with:
          build-args: --wfail
          lake-package-directory: LeanNumericalMethods
